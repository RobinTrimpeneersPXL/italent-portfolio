apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: portfolio-root
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/placeholder/italent.git # REPLACE WITH YOUR REPO URL
    targetRevision: HEAD
    path: INFRA/argocd/applications
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
